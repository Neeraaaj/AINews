import {makeStyles} from '@material-ui/core/styles';

export default makeStyles({
    media: {
        height: 240,
        borderRadius: 10,
    },
    typography: {
        fontFamily: 'Arial, sans-serif',
        h2: {
          fontWeight: 800,
          color: 'grey',
        }
    },
})